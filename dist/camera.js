var Camera = /** @class */ (function () {
    // TODO: add with and hieght / zoom once i understand this
    function Camera(x, y) {
        this.x = x;
        this.y = y;
    }
    return Camera;
}());
export { Camera };
